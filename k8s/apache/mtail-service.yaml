apiVersion: v1
kind: Service
metadata:
  name: apache-mtail
  namespace: demo-apps
  labels:
    app: apache
spec:
  selector:
    app: apache
  ports:
    - name: metrics
      port: 3903
      targetPort: 3903
